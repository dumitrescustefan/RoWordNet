language: python
python:
  - "3.5"
#  - "3.6"
#  - "3.7"
install:
  - pip install -r requirements.txt
# command to install dependencies
script: python rowordnet/unit-tests.py
branches:
  only:
    - master